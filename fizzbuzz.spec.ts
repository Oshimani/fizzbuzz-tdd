import { describe, it, expect } from "vitest";
import { fizzBuzz } from "./index";

describe("fizzbuzz", () => {
  const inputArray = Array.from(Array(100).keys());

  it("should output the numbers of the array one by one", () => {});

  it('should output "Fizz", when the number is divisible by 3', () => {});

  it('should output "Buzz", wenn the number is divisible by 5', () => {});

  it('should output "FizzBuzz", when the number is divisible by 3 and 5', () => {});
});
